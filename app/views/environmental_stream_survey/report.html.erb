<div class="active-scaffold-header">
  <div class="actions">
    <a href="<%= url_for :action => 'report', :format => 'csv' %>" class="action download"><%= :download_action.l %></a>
  </div>
</div>
<%= @report_html %>