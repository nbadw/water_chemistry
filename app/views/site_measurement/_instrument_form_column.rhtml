<dl>
  <dt>
    <label for="record[<%= column.name %>]"><%= column.label %></label>
  </dt>
  <dd>
    <%= select("record", column.name, Instrument.find(:all).collect{ |i| [i.name, i.id] }, { :include_blank => true }, { :id => column.name }) %>    
  </dd>
</dl>